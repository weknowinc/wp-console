commands:
  aliases:
    generate:plugin:
      - gp
    plugin:activate:
      - pa
    plugin:deactivate:
      - pd
    plugin:debug:
      - pde
    multisite:debug:
      - md
    multisite:new:
      - mn
    multisite:install:
      - mi
    site:install:
      - si